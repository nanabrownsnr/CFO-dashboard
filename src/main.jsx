import React from 'react';
import ReactDOM from 'react-dom/client';
import CFOCommandCenter from './CFOCommandCenter.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CFOCommandCenter />
  </React.StrictMode>,
);
